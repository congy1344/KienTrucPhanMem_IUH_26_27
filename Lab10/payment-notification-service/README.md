# Payment + Notification Service

```bash
npm install
npm start
```

Runs on `http://localhost:3004`.

## APIs

- `GET /health`
- `POST /payments`
- `GET /payments`

When payment succeeds, this service updates the order status and logs a notification.

